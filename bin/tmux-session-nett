#!/usr/bin/env bash

function tmux-nett {
  tmux start-server

  tmux net-session -d -s rails -n rails
  tmux net-session -d -s thin -n thin
  tmux net-session -d -s sidekiq -n sidekiq
  tmux net-session -d -s redis -n redis
}
