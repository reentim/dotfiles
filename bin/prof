#!/usr/bin/env bash

main() {
  local profile="$1"
  if [ -z "$profile" ]; then
    local profile="$(list-profiles | selecta)"
  fi

  [ -z "$profile" ] && exit 1

  change-profile "$profile"
  [ $TMUX ] && tmux-apply-colorscheme "$profile"
  return 0
}


main "$*"
