#!/usr/bin/env bash

if [ -z "$1" ]; then
  echo "Usage: $0 <application> [args...]"
  exit 1
fi

app="$1"
shift

if ! which "$app" &> /dev/null; then
  echo "ERROR: $app not found in PATH"
  exit 1
fi

nohup "$app" "$@" &> /dev/null &
