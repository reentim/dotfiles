#!/usr/bin/env bash

alias ls='ls -CFGtr'

if [[ $(uname) == "Darwin" ]]; then
  if (which gls > /dev/null); then
    alias ls="gls -tr --color=auto --group-directories-first --file-type --time-style=+'%F %T'"
  fi

  if (which gstat > /dev/null); then
    alias numfmt="gnumfmt"
  fi
fi

alias bx='bundle exec'
alias cdr='cd $(git rev-parse --show-cdup 2>/dev/null)'
alias diff='colordiff -u'
alias duh='du -csh'
alias grep='grep --color=auto'
alias l='ls -1tr'
alias ll='ls -ltr'
alias la='ls -latr'
alias rspec='rspec --color --tty --format documentation'
alias sr='screen -r'
alias sshu='ssh -A -l ubuntu '
alias t='tmux'
alias ta='tmux attach'
alias tree='tree -FC --dirsfirst'
alias vi='vim'
alias mytop='top -s 10 -o CPU -O mem -stats pid,command,cpu,time,threads,state,user -user `whoami`'

(which hub > /dev/null 2>&1) && alias git='hub'
